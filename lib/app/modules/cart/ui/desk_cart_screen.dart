import 'package:flutter/material.dart';

class DeskCartScreen extends StatelessWidget {
  const DeskCartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
