import 'package:flutter/material.dart';

class DeskNotificationScreen extends StatelessWidget {
  const DeskNotificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
